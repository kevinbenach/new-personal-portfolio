---
import Card from '@/components/ui/Card.astro';
import AboutText from '@/components/islands/AboutText.tsx';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<section id="about" class={`py-20 bg-white dark:bg-gray-900 ${className}`}>
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="max-w-6xl mx-auto">

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">

        <!-- Profile Image -->
        <div class="order-2 lg:order-1">
          <Card variant="elevated" hover class="p-2 max-w-md mx-auto">
            <div class="aspect-square rounded-lg bg-gradient-to-br from-primary-400 to-accent-400 flex items-center justify-center">
              <!-- Placeholder for profile image -->
              <div class="w-full h-full rounded-lg bg-gray-100 dark:bg-gray-800 flex items-center justify-center">
                <svg class="w-32 h-32 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
              </div>
            </div>
          </Card>
        </div>

        <!-- About Text Island - automatically updates with language changes -->
        <AboutText client:only="preact" />
      </div>
    </div>
  </div>
</section>